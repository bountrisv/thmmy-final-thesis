"aabbx";
WebInspector.ElementsTreeElement.parseChildren = function (fileNameBeforeReplace) {
    this.name = name;
    this.name = parseName(name, "x");
    if (!this._absoluteRegex)
        this._htmlEditsExpanded = true;
}
WebInspector.CodeMirrorResults.prototype = {
    reset: function () {
        this._addPlainHeaders(this);
    },
    componentLayout: function (registerElement) {
        this._logErrorMessage = window.InvalidError;
        this._showCompiler.registerElement(getFragment, registerElement);
        this._title = registerElement;
        this._killBehaviorsInNavigating = true;
        this._createView();
    },
    _enableCandidate: function (event) {
        if (!WebInspector.extensionAPI.ResourceContainer.excludeOnDominated())
            return;
        this._auditSearchResult.viewFactory = eventData;
        this._executionContextsDefaultAction = active + (event.data.name >= WebInspector.isSettingsUI.isControlled() ? event.section : WebInspector.UIString("Back"));
    },
    __proto__: WebInspector.VBox.prototype
}